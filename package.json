{
  "name": "auxstudio",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist && cp -r index.html *.svg *.ico *.png *.webmanifest dist/",
    "deploy": "npm run build && npx wrangler pages deploy dist --project-name=auxstudio --commit-dirty=true"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
